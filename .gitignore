# Build/Generated
/target
/foton-registry/src/generated
/foton-utils/src/generated
/foton-core/src/behavior/generated
/foton-core/src/entity/generated
/foton-worldgen/src/generated
/foton-utils/build_assets/builtin_datapacks
/foton-utils/build_assets/.asset-extract.lock
/foton-utils/build_assets/en_us.json
/site/dist

# Config
/config
/.tmp
/.logs
/world
/saves
/players
/script/.gitcraft-tmp
# Server directories the dev/*-test.sh scripts boot a server in.
/run/
/run-*/

# Misc
CLAUDE.md
.env
/minecraft-src
# Agent tooling scratch, except the skills the repo itself ships: an entry
# point that is not versioned only exists on the machine that wrote it.
.agents/*
!.agents/skills/
skills-lock.json
docs

# Package
profile.json.gz
profile.json.syms.json
*.gz
*.class

# ICE crash reports
rustc-ice-*

# IDE
.vscode/settings.json
.idea

.DS_STORE

# Python bytecode from the dev scripts
__pycache__/
.vercel

# The plugin API jar is built, and the libraries a plugin expects a
# server to provide are other people's artifacts. Neither is source.
plugin-api/build
/.superpowers

# Ruflo tooling state, written into the working tree by the plugin's hooks.
.claude-flow/

# Claude Code local session state; the commands under .claude/ are versioned.
.claude/proven-config.json
.claude/.proven-config-version

# Claude Code local session state; the commands under .claude/ are versioned.
.claude/proven-config.json
.claude/.proven-config-version
