package org.bukkit.inventory;

/** Flags controlling which item details are hidden from clients. */
public enum ItemFlag {
    HIDE_ATTRIBUTES,
    HIDE_ENCHANTS,
    HIDE_ARMOR_TRIM,
    HIDE_DESTROYS,
    HIDE_DYE,
    HIDE_UNBREAKABLE,
    HIDE_POTION_EFFECTS,
    HIDE_PLACED_ON
}
