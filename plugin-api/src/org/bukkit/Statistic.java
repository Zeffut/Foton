package org.bukkit;

/** Vanilla player statistics used by the Bukkit API. */
public enum Statistic {
    JUMP,
    /** Ticks played; vanilla's `play_time`, which it once called `play_one_minute`. */
    PLAY_ONE_MINUTE,
    TIME_SINCE_REST
}
