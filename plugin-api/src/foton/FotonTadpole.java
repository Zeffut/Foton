package foton;

import java.util.UUID;

/** Tadpole entity handle backed by Steel's ageable state. */
public final class FotonTadpole extends FotonLivingEntity implements org.bukkit.entity.Tadpole {
    public FotonTadpole(UUID id) { super(id); }
}
